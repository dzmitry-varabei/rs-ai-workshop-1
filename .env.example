# Supabase Configuration
# Copy this file to .env and fill in your values
# 
# To get these values:
# 1. Go to your Supabase project → Settings → API
# 2. Copy "Project URL" → VITE_SUPABASE_URL
# 3. Copy "Publishable key" → VITE_SUPABASE_ANON_KEY
# 4. Copy "Secret key" (default) → SUPABASE_SERVICE_ROLE_KEY

# Supabase Project URL (found in Project Settings → API → Project URL)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Publishable (anon) key (from API settings → Publishable key)
# This key is safe to use in browser/client-side code
VITE_SUPABASE_ANON_KEY=your-publishable-key-here

# Supabase Secret (service_role) key (from API settings → Secret keys → default)
# Keep this secret! Only use in server-side code (Telegram bot, etc.)
SUPABASE_SERVICE_ROLE_KEY=your-secret-key-here
